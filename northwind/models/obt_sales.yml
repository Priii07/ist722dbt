version: 2

models:
  - name: obt_sales
    description: The order sale fact table combined with all of its outrigger dimensions into one big table. Transaction fact table with one row per line item ordered. Includes product data.
